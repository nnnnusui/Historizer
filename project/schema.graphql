type Query {
    paragraphs: [Paragraph!]!
    paragraph(id: ID!): Paragraph
}

type Mutation {
    editParagraph(paragraphId: ID!, from: Int!, to: Int!): Paragraph!
}

type Paragraph {
    id: ID!
    content: String!
}
