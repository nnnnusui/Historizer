type Query {
    paragraphs: [Paragraph!]!
    paragraph(id: ID!): Paragraph
}

type Mutation {
    editParagraph(id: ID!, offset: Int!, length: Int!, to: String!): Paragraph
}

type Paragraph {
    id: ID!
    content: String!
}
